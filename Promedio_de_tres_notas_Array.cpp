#include<iostream>
#include<conio.h>

using namespace std;

int main()
{
	//Declarar array
	int d1[3];
	
	//Entrada de valores en el vector
	for(int i=0; i<=2; i++)
	{
		cout<<"Ingrese el valor de la nota #"<<(i+1)<<": ";
		cin>>d1[i];
	}
	
	//Salida datos
	for(int i=0; i<=2; i++)
	{
		cout<<"el valor de la nota #"<<(i+1)<<" es: "<<d1[i]<<endl;
		cout<<"El promedio de las notas es: "<<
	}	
} 
